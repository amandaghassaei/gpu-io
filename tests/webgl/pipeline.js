{
	const {
		GLSL1,
		GLSL3,
		WEBGL1,
		WEBGL2,
		NEAREST,
		LINEAR,
		REPEAT,
		CLAMP_TO_EDGE,
	} = WebGLCompute;

	const DIM = 30;

	// describe('pipeline', function () {
	// 	describe('readonly', () => {
	// 		it('should work for WebGL2 + GLSL3', () => {
	// 			console.log(testArrayReads({
	// 				TYPE: NEAREST,
	// 				DIM_X: DIM,
	// 				DIM_Y: DIM,
	// 				NUM_ELEMENTS: 1,
	// 				WEBGL_VERSION: WEBGL2,
	// 				GLSL_VERSION: GLSL3,
	// 				WRAP: CLAMP_TO_EDGE,
	// 				FILTER: NEAREST,
	// 			}));
	// 			assert.equal(1, 2);
	// 		});
	// 	});
	// });
}